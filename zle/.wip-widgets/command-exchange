# WIP

if [ ! $(which zsh) ]; then echo "You need zsh" && exit 1; fi

command_exchange () {
  local exchange_command=$(peco)
  local now_command="$BUFFER"
  BUFFER=$(echo "$now_command" | sed "s/view /${exchange_command} /g")
}

zle -N  command-exchange command_exchange

